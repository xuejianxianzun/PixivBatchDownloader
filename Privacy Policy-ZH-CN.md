# Powerful Pixiv Downloader 隐私政策(简体中文)

更新日期：2026-09-04

Powerful Pixiv Downloader(下称"本扩展")是一款开源的浏览器扩展，用于帮助用户在 pixiv.net 网站上批量下载作品。本扩展的源代码完全公开，您可以在 GitHub 仓库(<https://github.com/xuejianxianzun/PixivBatchDownloader>)中自行查看与审查。

本政策旨在说明：本扩展会访问哪些网站、在本地保存哪些数据、使用哪些浏览器权限，以及如何删除相关数据。**简而言之：本扩展不收集、不存储、不上传任何个人信息，不请求任何第三方网站，不包含广告、统计或追踪代码。**

## 一、数据收集与使用

本扩展**不收集任何个人信息**，包括但不限于：姓名、邮箱、账号密码、浏览记录、设备信息、位置信息等。

- 本扩展不包含任何统计、分析或埋点代码；
- 本扩展不包含广告服务；
- 本扩展不追踪用户的任何活动；
- 本扩展不会将任何数据发送给 pixiv 及其相关域名以外的任何服务器。

## 二、网络请求范围

本扩展只在以下 pixiv 相关网站上运行和发出网络请求，且均发生在 pixiv 页面内、由您的操作触发：

| 域名 | 用途 |
| --- | --- |
| `www.pixiv.net` | 抓取作品列表、作品信息、下载链接等数据；提交收藏/点赞等您主动执行的操作 |
| `www.pixivision.net` | 抓取 pixivision 特辑页面中的作品 |
| `i.pximg.net` / `s.pximg.net` | 图片等资源的实际下载地址 |

除上述 pixiv 系域名外，本扩展**不会请求任何其他网站**(例如不会请求任何更新检查服务器、不会请求任何第三方接口)。您在本扩展界面中看到的赞助、帮助文档等链接，均为普通超链接，只有在您主动点击时才会在浏览器中打开。

## 三、本地存储

本扩展会将必要的数据保存在**您自己的浏览器本地**，不会上传到任何服务器。主要包括：

- **设置项**：您在设置面板中做的各项配置；
- **下载任务记录**：批量下载任务的相关数据；
- **token 缓存**：见下文"Pixiv token"一节；
- **临时缓存**：例如标签列表缓存、文字替换列表、版本信息等，用于功能正常运行；
- **自定义背景图片**：如果您使用了自定义背景功能，图片会保存在浏览器的 IndexedDB 中，仅用于在本机显示。

上述数据全部存储于本地。卸载本扩展后，扩展自身保存的数据会被清除(见"数据删除"一节)。

## 四、Cookie

- pixiv 网站会使用 cookie 保存登录状态等信息；
- 本扩展**不会创建、不会修改、不会读取、也不会存储** cookie；
- 当本扩展在 pixiv 页面内发送请求时，cookie 由浏览器根据同源策略**自动附带**，本扩展不接触其内容。

## 五、Pixiv token

- pixiv 会为页面生成一个请求令牌(token)，部分网络请求需要携带它；
- 本扩展会从 pixiv 页面中获取该 token，并缓存在 pixiv 域名下的浏览器本地存储(localStorage)中，用于后续请求；
- 该 token 只会在向 pixiv.net 发起的请求中使用，**绝不会发送给 pixiv 以外的任何网站**；
- 如需清除该缓存，可在扩展设置中执行"重置设置"，或清除 pixiv 网站的站点数据。

## 六、浏览器权限说明

本扩展申请了以下浏览器权限，用途如下：

| 权限 | 用途 |
| --- | --- |
| `downloads` | 将您选中的作品保存到本地磁盘 |
| `storage` | 在本地保存设置与下载任务记录 |
| `webRequest`、`declarativeNetRequestWithHostAccess` | 仅在 pixiv 图片域名(pximg.net)范围内调整请求头(如 Referer)，使图片能够正常直链下载；不读取、不修改任何请求内容 |

## 七、第三方共享

- 本扩展**不与任何第三方共享数据**，也**不出售任何数据**；
- 本扩展不包含第三方 SDK；
- 本扩展的全部处理均在本机完成。

## 八、数据删除与用户权利

您可以随时通过以下方式删除本扩展产生的数据：

1. 在扩展设置面板中执行"重置设置"，清除扩展保存的设置与缓存；
2. 在浏览器的扩展管理页面中卸载本扩展；
3. 清除 pixiv 网站(www.pixiv.net)的站点数据，可移除保存在该域名下的 token 等缓存。

由于本扩展不将数据上传到任何服务器，因此不存在需要向服务器申请删除的数据。

## 九、免责与合规声明

- 本扩展是一个下载工具，下载行为与下载内容由用户自行负责；
- 请遵守 pixiv 的服务条款，尊重画师/作者的著作权，下载内容请仅用于个人学习、欣赏等合法用途，请勿用于商业用途或未授权转载；
- 本扩展为开源、免费软件，按"原样"提供，不对任何因使用本扩展而产生的后果承担责任。

## 十、政策更新与联系方式

- 本政策可能会随扩展功能的变化而更新，更新后会在本文档中注明日期；
- 如果您对本政策或本扩展的隐私实践有任何疑问，欢迎通过 GitHub Issues 提出：<https://github.com/xuejianxianzun/PixivBatchDownloader/issues>。
