# 设置面板重设计方案

日期：2026-05-17

## 1. 目标

在**保留所有现有设置、按钮、下载区域、动态插槽**的前提下，重新组织设置面板里的元素布局，让它同时解决下面几个问题：

1. 设置太多，扫一眼时难以区分。
2. 当前 3 个选项卡的信息架构不稳定，同一类设置被拆散到了不同地方。
3. 下载进度区域和下载按钮被压在第二个选项卡里，开始下载后不够醒目。
4. 置顶功能虽然实用，但会进一步打散原有分类。
5. 当前面板更像“把所有设置排成一列”，效率高，但辨识度、层次感、可维护性都偏弱。

我的结论是：**最值得采用的方向，不是继续微调现在的 3 个横向选项卡，而是改成“左侧导航 + 右侧工作区”的结构。**

---

## 2. 已确认的约束

这次重设计不应该破坏下面这些既有能力：

- 保留所有现有设置项，不删功能。
- 保留现有按钮区、下载区、进度条区。
- 保留动态插槽，因为最终 HTML 是由多个模块拼出来的。
- 保留“置顶选项”机制。
- 保留不同页面类型下的差异显示逻辑。
- 保留移动端适配能力。

换句话说，这次改的重点不是业务逻辑，而是**信息架构、布局层级、视觉组织方式**。

---

## 3. 我对现状的判断

### 3.1 目前的问题不只是“选项太多”

真正的问题其实有 4 个层面：

| 层面     | 当前情况                                     | 带来的问题                                         |
| -------- | -------------------------------------------- | -------------------------------------------------- |
| 一级结构 | 现在是 Crawl / Download / More 3 个选项卡    | More 承担了过多职责，像一个“杂物间”                |
| 二级结构 | More 里再分 5 个分类                         | 一部分同类设置又被拆到前两个选项卡，分类逻辑不稳定 |
| 行级结构 | 每个 option 基本都只是横向一行               | 当行内控件一多，就会变成一长串文字和开关           |
| 状态结构 | 置顶、高级设置、下载中、新功能等状态是叠加的 | 用户能操作，但不容易形成清晰心智模型               |

### 3.2 当前界面最核心的优点不能丢

虽然现在不够美观，但它有一个非常强的优点：**高频操作离结果很近**。

例如：

- 抓取相关按钮就在抓取设置附近。
- 下载相关设置和下载进度在同一个大区域。
- 大多数设置都能直接改，不需要经过太多层跳转。

所以重设计不能走成“更漂亮，但操作路径更长”。  
我的建议是：**保留效率优先，但把效率建立在更清晰的结构上，而不是建立在把所有东西直接摊开上。**

### 3.3 旧的“显示高级设置”机制不必保留

旧方案里之所以需要“显示高级设置”，本质上是因为：

- 设置项堆在一个较长的列表里。
- 用户很容易被过多设置淹没。

但在新的结构里，已经有：

- 一级导航
- 二级分组
- 首页（仪表盘）
- 置顶设置

这些机制已经足够承担“减轻认知负担”的职责。  
因此，后续重构时不必继续保留“显示高级设置 / 简洁模式”这一套逻辑，更合适的替代方案是：**保留全部设置，只控制所有二级分组的展开 / 折叠状态。**

---

## 4. 重设计时应该坚持的原则

### 4.1 先解决“找不到”，再解决“好不好看”

优先级应该是：

1. 让用户更快找到设置。
2. 让用户更容易理解某个设置属于哪一类。
3. 让下载状态更容易被看到。
4. 最后再做视觉美化。

### 4.2 一级分类要稳定，二级分组要贴近任务

一级分类不应该再出现“这类设置的一部分在 A，一部分在 B”的情况。  
一级分类一旦确定，就尽量稳定。

二级分组则应该更贴近用户任务，例如：

- “筛选抓取范围”
- “图片过滤”
- “命名规则”
- “保存格式”
- “下载行为”

而不是只按代码模块或历史演化痕迹分组。

---

## 5. 新的方案：标题栏 + 左侧导航 + 右侧工作区

这是我最推荐的方案。  
它比现在改动大一些，但收益也最大，而且最能同时兼顾：

- 结构清晰
- 高频操作效率
- 下载状态可见性
- 后续继续加设置时的可扩展性

### 5.1 总体框架

建议把整个面板改成下面这种结构：

```text
┌──────────────────────────────────────────────────────────────┐
│ 标题栏：logo、名称、搜索框、展开/折叠全部、GitHub、Wiki、赞助、关闭  │
├──────────────┬───────────────────────────────────────────────┤
│  首页        | 首页：置顶设置 + 抓取按钮区 + 其他按钮区 + 下载区 │
│  抓取        │ 抓取分类内容区                                  │
│  命名        │ 命名分类内容区                                  │
│  下载        │ 下载分类内容区                                  │
│  增强        │ 增强分类内容区                                  │
│  其他        │ 其他分类内容区                                  │
│  帮助        │ 帮助入口区                                      │
│  搜索        │ 搜索结果区域（用户搜索时才会显示）               |
├──────────────│───────────────────────────────────────────────┤
│  下载摘要栏   │ 显示总进度、暂停下载、停止下载等简要状态         │
└──────────────────────────────────────────────────────────────┘
```

#### 标题栏的布局

左右两端分布：
- 左侧是 logo（文件是 `\src\static\icon\logo128.png`）和名字
- 右侧是搜索框等各种按钮、控件。其中又分为 2 个区域：搜索框和“展开/折叠全部”位于一个容器里，右侧的其余按钮都放在第二个容器里。第二个容器只在 PC 端页面里显示，在移动端页面里隐藏。这样是为了便于在移动端里分开处理这两个区域。
- 添加一个单独的关闭按钮，定位到设置面板的右上角，只在移动端显示，用来确保关闭按钮位于右上角。

#### 主体区域的结构

有几个关键变化：
1. **分类导航从顶部横向改成左侧纵向。**
2. 由于添加了左侧区域，所以设置面板需要适当加宽，这样右侧显示按钮、设置的区域依然可以使用与之前一致或接近的宽度，不会因为宽度变小而产生更多的换行。
3. **首页（仪表盘）默认显示**：置顶设置和高频功能都集中在首页，以减少初次打开面板时的操作成本。置顶的设置、抓取按钮区域默认展开；其他按钮区域和下载区域默认折叠。有下载任务时自动展开下载区域。
4. **下载状态全局化**：在左侧导航区底部添加一个简单的显示下载状态摘要的区域，默认不显示内容，当下载开始后显示：下载总进度（以文字形式显示即可）、暂停下载与停止下载按钮。后两个按钮不是必须的。该区域保持简洁，不要放太多信息。

#### 对一级分类的处理

- 同时只能激活一个一级分类。
- 需要记录上一次激活的分类、当前激活的分类，在有需要时使用。当搜索页面隐藏之后（因为用户清除了搜索内容），需要自动切换到上一次激活的分类。至于当前激活的分类，可能会在一些代码逻辑里使用。
- 每当激活左侧的一级分类时，隐藏其他分类的右侧区域，并显示当前分类的右侧区域。还需要检查它里面应该显示哪些设置项。如果某个设置项应该显示在这里但却没有显示（因为它可能被移动到了搜索结果里），就把这个设置项移动过来。
- 被置顶的设置只会显示在“首页”和搜索结果里，不会显示在其他一级分类里。在其他分类里不需要显示该分类里被置顶的设置。
- 左侧的“搜索”分类平时不会显示，所以用户不能主动激活它。只有当需要显示搜索结果时，会自动激活并显示它，并在它的右侧区域显示搜索结果。

#### 搜索功能

##### 搜索框的样式

在左侧显示一个搜索图标（不可点击，因为不需要点击它）。当输入文字之后，在右侧显示一个 x 图标，点击它可以清除输入的搜索词。示意图如下：
![搜索框示意图](./20260518_231835.png)

##### 搜索匹配范围

- 从所有设置项的配置数据里查找（而不是从 DOM 元素里查找）。不查找“帮助”里的内容，因为它里面的内容不属于设置。
- 查找每个设置项的名字。需要注意的是，设置名字有多语言版本，只从与当前界面语言相同的名字里查找，不要查找其他语言版本的名字。备注：设置名字的字符串里可能含有 html 标签（通常是 span 标签），所以需要在设置的数据里添加一个属性来保存过滤了 html 标签后的名字。
- 查找每个设置的关键字。这是因为有些设置的名字里含有一些同义词，用户可能输入了同义词，此时如果关键字里有同义词的话就可以搜索到这个设置了。

##### 如何呈现搜索结果

- 用户输入搜索词时，自动查找并显示搜索结果。为了避免短期内多次计算的压力，需要使用防抖处理（等用户停止输入 0.2 秒之后才开始查找搜索结果）。
- 不在搜索框下面显示下拉的候选项列表，因为这样会增加复杂度。而且目标设置本身就可以在设置面板里显示，所以没太大必要显示下拉候选项。
- 当有搜索结果时，激活“搜索”分类，并在其右侧内容区域（全局搜索结果页）里显示符合条件的设置。
- 搜索结果可能会来自多个一级分类，此时把搜索到的设置项从它们的二级分类区域里移动到搜索结果区域里。并且以“一级分类”+“二级分类”为搜索结果进行分组，例如 `下载 / 文件格式`。
- 在呈现搜索结果时，这些设置的显示顺序应该与左侧一级导航一致：依次是：首页 → 抓取 → 命名 → 下载 → 增强 → 其他。
- 符合条件的设置项可以直接使用，所以点击它们时，不需要跳转到它们原本所属的一级分类里。

当搜索结果页面显示之后，如果用户清除了搜索词，就隐藏该页面，并重新激活上一次激活的一级分类。

##### 搜索结果页的 3 种状态

 1. 未搜索：搜索分类隐藏，并激活上一次的一级分类页面
 2. 搜索并且有结果：显示搜索结果并分组  
 3. 搜索并且无结果：显示空状态提示，例如“没有找到符合条件的设置”

退出搜索页面的动作可以由多种触发方式：
- 当用户点击搜索框右侧的 x 图标时会清除搜索词，此时退出搜索页面
- 当用户手动清空了搜索词时（例如连续按 backspace 键）。可以通过监控搜索框 input 控件的 value 来执行这个检测逻辑。

#### 移动端的显示样式

移动端的标题栏分成 3 行：
- logo、名称
- 搜索框和“展开/折叠全部”
- 隐藏剩余的按钮区域
- 显示移动端关闭按钮（它会显示在右上角，保持了位置与 PC 端一致）

移动端把中间部分（左侧导航区域+右侧分类内容区域）改成**上下布局**，而不是继续保留左右两列：

- 一级导航改成横向排列。
- 当前分类内容显示在最下方。

这样可以避免在窄屏上左右两列都过窄，导致大量换行。

### 5.2 一级导航建议改成 7 类

我建议把现在 More 里的 5 个大类真正提升为一级导航，这样结构会稳定很多。另外我还添加了 2 个额外的一级导航：“首页”和“帮助”。

| 一级导航 | 主要承载内容                                                                                            | 说明                                                                                 |
| -------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 首页     | 置顶的设置、抓取按钮区域、其他按钮区域、下载区域                                                        | 该分类里的内容相当于仪表盘首页。该分类会默认显示                                     |
| 抓取     | 抓取范围、过滤条件、抓取策略、抓取按钮                                                                  | 对应“决定抓什么”                                                                     |
| 命名     | 文件名规则、文件夹规则、序号、标签别名                                                                  | 对应“决定文件叫什么”                                                                 |
| 下载     | 保存格式、下载线程、下载策略、下载触发                                                                  | 对应“决定如何保存和下载”                                                             |
| 增强     | 缩略图、预览、复制、高亮、界面增强                                                                      | 对应“增强使用过程”                                                                   |
| 其他     | 外观、语言、日志、导入导出、设置管理                                                                    | 系统级、全局级选项                                                                   |
| 帮助     | 显示原本位于下载器底部的帮助按钮和链接：常见问题 使用手册 GitHub 获取帮助 最近更新 Fanbox 下载器 赞助我 | 帮助信息和一些其他链接。它应该与上面的分类有视觉区分，因为它右侧显示的并不是设置项。 |

### 5.3 样式方面的一些设想，以及卡片的设想

参考 Clash Verge 的配色方案（截图文件是 `\notes\settings panel\20260518_024810.png`）：
- 设置面板的左侧、右侧区域可以使用不同灰度的背景颜色来区分。
- 右侧的按钮区域、设置项都可以添加白色背景，并添加 padding、margin，使每个按钮区域或设置项都显示为独立的小卡片。这有助于设置项挨在一起时不容易区分开的问题：之前所有设置项都是直接显示文字，排列在一起时很难分辨不同设置的边界。做成小卡片之后就很容易分辨了。

附加说明：
1. 卡片要轻，不要做成太厚重的仪表盘风格。因为设置数量很多，如果每个设置卡片阴影很重、边框很深、圆角很大，界面会显得碎。更稳的是：
  - 浅色背景
  - 很轻的边框或阴影
  - 小圆角
  - 主要靠留白和分组来区分
  - 每个卡片之间有一定间距
2. 卡片分为这些种类：
   - 分类卡片（带标题）：用来显示右侧二级分组的标题，文字加粗加大显示。在右侧还有三角形箭头表示展开、折叠状态。点击分类卡片会切换其下内容区域的展开/折叠状态。
   - 全宽度卡片：用于直接显示内容的卡片。没有标题、三角箭头等结构，只是单纯的卡片。根据它是否隶属于分类卡片，其宽度会有区别：如果隶属于分类卡片，那么它的宽度比分类卡片少窄一些，以表示层级关系。如果它不属于分级卡片，那么就是全宽度。
   - 部分宽度的卡片：用于直接显示内容的卡片。没有标题、三角箭头等结构，只是单纯的卡片。它只占据部分宽度，通常用来显示小的按钮、超链接，所以不需要使用全宽度。不需要为它单独做一个卡片类型，因为只要为这些卡片的父元素设置 Grid 布局即可让卡片只占据部分宽度。
3. 右侧的卡片如何显示：
  - “首页”的右侧内容区域：有 4 个子区域，每种区域都是分类卡片+全宽度卡片的组合。
  - 中间 5 个设置分类的右侧内容区域：里面的内容是多个二级分类的设置，每种分类都是分类卡片+全宽度卡片的组合。
  - “帮助”分类：右侧的区域不是设置项，而是多个小按钮或超链接。所以它右侧没有分类卡片，只有多个占据了部分宽度的卡片。

### 5.4 置顶的设置如何处理

我在左侧导航区域添加了“首页”分类，它右侧的内容区域里会显示置顶的设置。

**被置顶的设置只有一份对应的设置元素：**

当左侧导航区域激活了“首页”分类时，查找所有被置顶的设置，把它们的元素从其原本所属的分类区域里移动到“首页”的置顶设置区域里。

当左侧导航区域激活了非“首页”分类时，则查找“首页”分类里是否有该分类下的设置，如果有则把这个设置从“首页”分类区域里移动到当前分类区域里。这样，用户在其他分类里依然能看到被置顶的设置，不会产生“这个设置怎么不见了”的困惑。

这样做的目的是为了让置顶的设置只有一份元素，而非多份。原因是下载器在设计时没有考虑到一个设置会存在多份的情况。如果一个设置存在多份，会带来问题：
- 由于设置项都包裹在一个 form 表单里，并且使用 form.key 的方式来读取、设置对应的值。如果被置顶的设置存在多份，那么同一个 key 会出现多次，造成问题。
- 一些模块需要向特定的设置里添加子元素，如果有多个相同的设置，就需要遍历多个设置来设置数据、创建子元素，会比较麻烦。

### 5.5 每个一级导航内部，再拆成“任务型分组”

下面是我建议的二级分组方式。

#### 抓取

| 分组           | 内容举例                                               |
| -------------- | ------------------------------------------------------ |
| 基础范围       | 抓取多少作品、抓取多少页面、抓取每个用户最新作品       |
| 作品类型       | 插画/漫画/动图/小说、年龄限制、AI、原创/非原创         |
| 收藏与关系过滤 | 收藏状态、收藏数、日均收藏、用户屏蔽、特定用户屏蔽 tag |
| 图像条件过滤   | 宽高、比例、尺寸、体积、图片数量、多图前后若干张       |
| 标签与标题过滤 | 需要 tag、不需要 tag、标题包含/排除、系列标题参与      |
| 执行策略       | 减慢抓取速度、定时抓取、自动导出结果、导出 ID          |

#### 命名

| 分组         | 内容举例                                                |
| ------------ | ------------------------------------------------------- |
| 命名规则     | 图像命名规则、小说命名规则、按页面类型使用不同规则      |
| 文件夹规则   | 多图加文件夹、R18 文件夹、按 tag 建文件夹               |
| 特殊命名逻辑 | 标签别名、含特定 tag 用不同规则、用户名处理、去除 emoji |
| 序号与长度   | 序号起始值、补零、文件名长度限制、是否省略序号          |
| 命名辅助操作 | 保存命名规则、导入/选择规则                             |

#### 下载

| 分组           | 内容举例                                               |
| -------------- | ------------------------------------------------------ |
| 基础下载行为   | 下载线程、自动开始下载、下载间隔、保存到上次位置       |
| 文件格式       | 图片尺寸、动图保存格式、动图转换线程、小说保存格式     |
| 内容附加保存   | 保存作品元数据、保存作品简介、小说元数据、封面、内嵌图 |
| 下载策略       | 去重、下载顺序、点击收藏/点赞时下载、不下载已下载作品  |
| 系列与小说     | 自动合并系列小说、合并阈值、是否跳过单独下载           |
| 下载记录相关   | 下载记录、结果文件、导出相关逻辑                       |

#### 增强

| 分组         | 内容举例                                               |
| ------------ | ------------------------------------------------------ |
| 预览         | PreviewWork、预览尺寸、等待时间、单图/多图/动图预览    |
| 缩略图与列表 | 更大缩略图、缩略图按钮、边框、列表显示、多图缩略图列表 |
| 复制与信息   | 复制按钮、复制格式、详细信息面板                       |
| 页面增强     | 高亮关注用户、搜索结果过滤、移除已关注用户作品等       |

#### 其他

| 分组       | 内容举例                                 |
| ---------- | ---------------------------------------- |
| 外观       | 主题、背景图、关键字高亮、选项卡切换方式 |
| 语言与提示 | 语言、帮助提示重新显示                   |
| 日志       | 默认可见性、导出日志、导出时机、日志过滤 |
| 设置管理   | 导入设置、导出设置、重置设置             |

这个分法的重点是：**二级分组不是按历史文件结构来分，而是按用户决策流程来分。**

### 5.6 单个设置项应该改成“卡片化行结构”

现在很多设置都是“一行内堆很多控件”，看起来像连续文本。  
建议把每个设置统一成下面这种结构：

```text
┌─────────────────────────────────────────────────────┐
│ 标题区：设置名 | 帮助 | wiki | 状态徽标（NEW / 已置顶） │
│ 主控件区：开关 / 单选 / 输入框 / 组合控件             │
│ 子设置区：仅在启用后展开，缩进显示                     │
└─────────────────────────────────────────────────────┘
```

具体建议：

- 每个设置块之间增加更稳定的垂直间距。
- 标题与控件分成两层，而不是永远并排。
- 有子设置的 option，子区域用浅背景、缩进、细边框区分。
- 帮助按钮和 Wiki 链接收拢到标题右侧，减少正文噪音。
- `NEW`、`📌` 之类状态不要再插在左侧悬空位置，改成标题区徽标更稳。

这样做以后，哪怕选项很多，扫描负担也会明显下降。

### 5.8 抓取按钮、下载按钮、其他功能按钮的摆放方式

按钮区不建议再简单放在设置流里，和普通设置项混排。  
目前的方案是把它们集中放在“首页”分类（仪表盘）里：

- **抓取按钮区**：显示开始抓取、停止抓取、选中作品相关按钮等高频操作。
- **其他按钮区**：显示不适合归入普通设置的其他功能按钮。
- **下载区**：显示下载相关按钮和详细进度条区域。

这样做的优点是：

- 操作逻辑统一：左侧导航负责切换右侧内容，不再有一套额外的“折叠/展开独立区域”逻辑。
- 首页默认显示，高频内容在打开面板后仍然是第一屏可见。
- 能显著减少标题栏下方长期挂着多个独立区域所占据的高度。

它的代价是：当用户切换到其他一级分类后，如果想使用这些高频功能，需要先切回首页。  
如果后续验证发现这点影响明显，可以再考虑给“开始抓取”补一个更轻量的全局快捷入口。

### 5.9 “显示高级设置”开关改为“展开/折叠所有二级分组”

因为现在每个一级导航（分类）里对设置项进行了二级分组，每个分组里的设置数量不太多，因此没必要默认隐藏部分设置了。

新的方案是：

- **不隐藏设置项本身**
- 只控制二级分组的展开 / 折叠状态
- 标题栏右侧提供 `展开/折叠全部` 按钮
- 这个按钮作用于所有二级分类，会跨全部分类统一展开或折叠二级分类

### 5.10 这个方案为什么最适合这个项目

因为这个项目的本质不是一个普通设置页，而是一个：

- 带运行状态的工具面板
- 带大量专业设置
- 同时要求高频操作效率很高

所以它更像：

- **轻量控制台**
- 而不是传统的“系统设置页”

用“左侧导航 + 右侧工作区”的方式，会比传统选项卡更适合这种工具属性。

## 7. 我最建议先落地的 5 个改动

如果不想一次改完全部，可以按收益排序分阶段做。

### 第 1 步：整理设置元数据

先把每个设置映射到：

- 一级导航
- 二级分组
- 是否支持置顶
- 搜索关键词

这是后续重构的基础。

### 第 2 步：实现新的外层骨架

先把面板外层结构改成：

- 标题栏
- 左侧导航
- 右侧内容区
- 左下角下载摘要栏

并确保移动端可以切换到上下布局。

### 第 3 步：实现首页（仪表盘）

把这些内容集中到首页：

- 置顶设置
- 抓取按钮区
- 其他按钮区
- 下载区

### 第 4 步：把设置页改成一级导航 + 二级分组

把原来分散在多个选项卡里的设置重组到新的一级导航和二级分组里，并支持单个二级分组的展开 / 折叠。

### 第 5 步：补卡片样式与全局展开/折叠

最后再统一做：

- 设置项卡片化
- 帮助页样式
- 全局的“展开全部 / 折叠全部”功能
- 搜索与筛选体验优化

---

## 8. 如果以后真的开始实现，我建议的代码层面拆法

虽然这份文档现在只是设计思路，但如果后续要落地，我建议从代码结构上这样拆：

### 8.1 先补一份“设置元数据表”

不要继续只靠 `data-no` 和写死顺序来表达结构。  
建议给每个设置补充元数据，例如：

- 所属一级导航
- 所属二级分组
- 是否被置顶。默认都是 `false`，用户手动把它置顶之后为 `true`。
- 搜索关键词

这样后面无论做：

- 分组折叠
- 搜索设置
- 置顶设置
- 当前页面过滤

都会容易很多。

### 8.2 HTML 层面要从“连续列表”改成“分区容器”

当前结构更像是一个连续流。  
后续应该变成：

- nav 容器
- title bar 容器
- home dashboard 容器
- section group 容器
- download summary 容器

### 8.3 置顶逻辑建议从“移动到选项卡顶部”改成“移动到首页里”

这样可以尽量少碰原有分类顺序，也更符合用户心智。

---

## 9. 最终结论

如果只用一句话概括我的建议，就是：

> 把当前设置面板从“3 个页签里的长表单”，升级成“带仪表盘（首页）、导航分类、全局下载状态”的轻量控制台。

其中最关键的 4 个点是：

1. **一级分类稳定化**：把 5 个设置分类提升为主导航，并补充“首页”和“帮助”两个特殊分类。
2. **在仪表盘里集中显示置顶的设置和多种按钮**：把这些重要的设置和功能集合起来，而不是分散在多个选项卡里。
3. **下载状态全局化**：在左侧导航区底部添加一个简单的显示下载状态摘要的区域，默认不显示内容，当下载开始后显示：下载总进度、暂停下载与停止下载按钮。
4. **设置项卡片化**：每个 option 都有更清晰的标题、控件、子设置层次。

如果你后续要我继续推进，我建议下一步不是直接写最终代码，而是先做两件事：

1. 先把所有设置项映射到“一级导航 + 二级分组 + 置顶支持”的结构表。
2. 再基于这张结构表，画一版更接近真实界面的低保真原型。
