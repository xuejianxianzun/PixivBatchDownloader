# 对设计稿的说明

日期：2026-05-24

我对设置面板进行了重新设计。一些界面元素是参考 Clash Verge 的界面设计的。

## 首页的设计稿

![](./设计图-首页.png)

这张图片展示了首页的设计稿。

顶部是标题栏，里面有 Logo、名称、搜索框、展开/折叠全部分组的按钮、赞助按钮、关闭按钮。

接下来是内容区域，左侧是一级分类导航，点击分类就会在右侧显示对应的页面，内容通常是设置项的列表。

第一个页面“首页”是整合了“置顶的设置”和主要按钮区域的仪表盘，它也是默认显示的页面。

其他页面里的内容类似于“置顶的设置”区域，有二级分类标题，其下是设置项的列表。

所有二级分类和按钮区域都可以折叠、展开，以避免占据太多高度。在默认情况下，只有“开始抓取”区域是展开的，因为它是任务开始的入口。“下载区域”会在抓取完成后、以及开始下载时自动展开。

界面的左下角显示了精简版的下载进度和控制按钮。不管你在查看哪个导航页面，都可以在左下角查看下载进度。

备注：
- 右侧的“置顶的设置”、“附加功能”区域默认是折叠的，在设计图里为了查看效果所以是展开的。
- 有些卡片具有浅蓝色边框，这表示置顶的设置。
- 有些卡片的右上角有 new 徽章，这表示它是近期新增的设置。
- 每个卡片里的内容是旧的设置面板上的截图，因为目前我没有计划对这些内容进行重新设计，所以维持了原样。以后可能做些微调。

## 搜索页面的设计稿

![](./设计图-搜索.png)

这张图片展示了用户搜索“小说”并且有搜索结果时的情景。

此时左侧的“搜索”导航被激活，并显示了 8 条搜索结果。
搜索结果按二级分类进行分组。
设置名称里的关键字具有高亮效果。

大部分页面里都会以类似的方式显示内容：二级分类标题 + 设置列表。

## 帮助页面的设计稿

![](./设计图-帮助.png)

这张图片展示了用户激活“帮助”导航时，右侧显示的内容。

顶部是几条提示消息。

下方是多个小卡片，每个卡片都是一个按钮。

## 新版界面解决了哪些问题

### 功能按钮太过分散

在旧版界面里，开始抓取、下载按钮、下载区域、更多按钮是分散在 3 个选项卡里的。默认显示的“抓取”选项卡只会显示开始抓取。

在新版界面里，添加了“首页”作为仪表盘，在这里集中展示所有重要的功能按钮和下载区域，使高频操作不再需要频繁切换选项卡。

### 设置项割裂

在旧版界面里，一些抓取设置分散在“抓取”和“更多”选项卡里。一些命名、下载设置也分散在“下载”和“更多”选项卡里。同一分类的设置分散在两个选项卡里导致设置割裂。

在新版界面里，同一分类的设置会集合在一起，不会割裂。

### 一级分类导航不够方便

在旧版界面里，大部分设置都放在了“更多”选项卡里，导致“更多”选项卡里保存而了全部的 5 个一级分类和数十个设置，但没有提供便捷的分类跳转功能，这导致用户很难快速定位到自己需要的一级分类。

在新版界面里，全部的 5 种设置分类都会显示在左侧，用户可以直接点击分类名字查看该分类下的所有设置。

### 没有二级分类

大部分一级分类都有超过 10 个设置，有些甚至达到了 30 个设置。

旧版界面里这些设置没有二级分类，太多的设置挨在一起，导致查找特定设置的效率很低。新版界面从一开始就设计了二级分组，方便用户快速定位到需要的设置。

### 没有搜索功能

下载器的设置数量已经接近上百个，旧版界面没有搜索功能，用户必须逐个查看设置并判断这是不是自己需要的功能。

新版界面添加了搜索功能，让用户在寻找特定设置时更加便捷。

### 设置项之间难以区分

在旧版界面里，所有设置项都是纯文字，从上到下排列。这导致在大段文字之中很难区分每一条设置的边界。

新版界面里每个设置都显示为独立的卡片，天然具有分界线。

### 不够美观

旧版界面基本上就是单纯的文字和按钮的混排，缺少美感和设计感，有些用户觉得界面复古。

新版界面以 Clash Verge 的界面布局、卡片设计为灵感，使界面的美观程度得到了很大提升。

## 新版界面的结构

新版下载界面在 PC 端的结构如下所示：

```text
┌──────────────────────────────────────────────────────────────┐
│ 标题栏：logo、名称 | 搜索框、展开/折叠全部、赞助、关闭           │
├──────────────┬───────────────────────────────────────────────┤
│  首页        | 首页：置顶设置 + 开始抓取 + 附加功能 + 下载区域   │
│  抓取        │ 抓取分类内容区                                  │
│  命名        │ 命名分类内容区                                  │
│  下载        │ 下载分类内容区                                  │
│  增强        │ 增强分类内容区                                  │
│  通用        │ 其他分类内容区                                  │
│  帮助        │ 提示信息、帮助按钮                              │
│  搜索        │ 搜索结果区域（用户搜索时才会显示）               |
├──────────────│───────────────────────────────────────────────┤
│  下载摘要栏   │ 显示总进度、暂停下载、停止下载等简要状态         │
└──────────────────────────────────────────────────────────────┘
```

## 重构时的约束

### 配色方案

标题栏的背景颜色是白色 `#fff`，导航栏的背景颜色是浅灰色 `#f5f5f5`，内容区域的背景颜色是稍微深一些的 `#eee`。

主题色是 `#199df5`。

图标按钮、输入框在 hover 和点击时，如无特别说明，那么它们的高亮颜色就是主题色。

内容区域的文字默认颜色是 `#444`。有些元素会设置更具体的颜色来覆盖这个默认值。

---------

下面这段文字保存了与主题色比较搭配的颜色，目前不需要使用。

蓝色 #199df5（一种明亮的亮天蓝色）最搭配的黄色是 #f5c519（明亮的金黄色/琥珀色）。这两者属于经典的互补色调搭配，色彩平衡度极高。

1. 经典互补搭配（对比强烈、富有活力）
- #f5c519：正能量的金黄色，与 #199df5 在色相环上几乎完全相对，视觉冲击力强，非-常适合用于按钮、高亮标签或图标。
- #ffd214：高饱和度的明黄色，适合打造年轻、动感、科技感或运动风的界面。

2. 柔和小清新搭配（舒适、耐看、不刺眼）
- #fff099：柔和的淡黄色，适合作为大面积背景色，与蓝色的主调搭配显得干净、温馨。
- #fbe183：奶油莫兰迪黄，降低了饱和度，视觉上非常高级、温和。

3. 高级感暗色调搭配（稳重、轻奢）
- #d4a311：暗金/芥末黄，降低了明度，与天蓝色搭配能产生一种沉稳、时尚的设计感。

#### 下载器的颜色主题

另外，下载器在不同的颜色主题下，会覆写一些设置的样式。可以参考 `src\style\settingsPanelV2.less`，里面的 `.centerWrap` 有 4 种状态：
- 单独的 `.centerWrap`，这是默认样式，其实也是亮色主题的样式，因为下载器最初只有亮色主题。你为新的设置面板设置样式时，主要是添加在这个选择器里面。
- `.centerWrap.theme-white`，这是亮色主题里的 patch，在默认样式的基础上单独调整一些样式。不过它里面覆写的样式不多，因为下载器的默认样式就是亮色主题。 
- `.centerWrap.theme-dark`，这是暗色主题里的 patch，在默认样式的基础上单独调整一些样式。
- `.centerWrap.xzBG`，这是用户启用了“背景图片”设置时的 patch（会在设置面板下方显示一张背景图片）。

在实际使用中，`.centerWrap` 一定会有 `.theme-white` 或 `.theme-dark` 其中的一个。
`.xzBG` 则是当用户启用了“背景图片“设置之后才会添加。它可以与颜色主题共存，并且实际上它相当于是一种特殊的颜色主题，会覆盖很多样式。

目前的设计稿是在亮色主题下的表现，没有制作其他主题下的设计稿。你可以参考已有的样式来设置一些元素在其他主题下的样式。通常来说，在除了亮色主题之外的主题里，背景颜色比较暗，所以设置面板上的文字会显示为浅灰色或白色。一些元素的背景颜色可能变成透明的或者半透明。一些投影的颜色也会从蓝色变成很淡的白色。

### 样式

所有元素都是 `box-sizing: border-box;` 盒模型。

内容区域里的正文文本字号是 `14px`。一级导航、分类标题等文字需要适当使用更大的字号，例如一级分类的名字的字号使用 `19px`，卡片的标题使用 `16px`。

文字默认都不加粗，除非特别说明需要加粗的。

### 内容和功能

设置面板的容器元素保持已有的 className。最外层元素依然是 `src\ts\CenterPanel.ts` 里的 `<div class="centerWrap ${'lang_' + lang.type}">`，因为有些功能依赖 `centerWrap` 这个类名。主体区域容器的 className 里要有 `centerWrap_con` 以保持样式的兼容性。

一些大的板块元素可能有滚动条（例如导航栏、内容区域），需要给它们添加 `beautify_scrollbar` 的 className 以显示美化的滚动条。

设置项的功能需要保持可用，不要因为重构而导致功能集体失效。重点是要为主体区域嵌套一层 `<form class="settingForm">` 元素，因为下载器内部通常是通过 form.key 来读写设置值的。

在内容区域里，大部分元素都是旧设置面板里的元素，只是放到了卡片里，以及或者修改了所属的分类。所以在重构时，对于卡片里的内容，优先复用已有的代码、样式和逻辑。这样很多逻辑（例如下载区域何时显示、其内部按钮的显示与隐藏）都不需要重新进行处理了。

每个设置的卡片元素需要和之前一样设置 `data-no="<编号>"` 属性，因为一些模块会使用这个数据属性来查找该设置的元素（卡片），对其执行隐藏、显示、移动节点等操作。

### 文本

有时我会明确说明一些按钮的 title 属性，或者一些文字内容。这些文本不能直接设置成固定的字符，需要使用多语言文本配置（可以参考旧代码中的例子）。

## 标题栏

### 内容

标题栏的内容分为 2 个大板块。

板块 1: 
- logo（文件是 `\src\static\icon\logo128.png`）
- 名字
- 只在移动端页面里显示的关闭按钮。它总是显示在这个板块的最右侧。当鼠标 hover 时，图标高亮。

板块 2:
- 第 1 个区域里有两部分内容：放置搜索框和“展开/折叠全部”按钮。搜索框在下面有详细说明。展开/折叠按钮只占据一个位置，根据当前状态显示展开图标或折叠图标，起到指示的作用。当鼠标 hover 时，图标高亮，颜色是主题色，并且变成另一种状态的图标，让用户知道下一步的操作是改变折叠状态。title 是“展开/折叠所有区域”。
- 第 2 个区域放置不重要的按钮，目前里面只有赞助按钮。点击赞助按钮的效果等于点击目前下载器底部的“赞助我”，会显示一个消息框来显示提示。这个区域只在 PC 端页面里显示，在移动端页面里隐藏，以减少占据的空间。当鼠标 hover 时，图标高亮，颜色是特别的紫色 `#e448a3`。title 是“赞助我”。
- 第 3 个区域放置只在 PC 端页面里显示的关闭按钮。当鼠标 hover 时，图标高亮。title 是“关闭”。

### 样式

#### PC 端的显示样式

在 PC 端，标题栏的两个板块是左右两端布局的。即板块 1 位于左侧，板块 2 位于右侧，显示在一横排。

logo 图片的宽高是 `24px`。名字的字号是 `18px`，加粗，颜色使用主题色。

标题栏的总高度是 `60px`，`padding: 15px 20px;`。其中的内容在水平轴上居中对齐。

搜索框的宽度是 `210px`。每个区域之间有 `15px` 的间距，区域内的每个元素之间也有 `15px` 的间距。

#### 移动端的显示样式

在移动端页面里，标题栏里的两个板块从上到下显示，每个板块占据 1 行。每行的高度是 `32px`，内容都是居中显示的。

在移动端里，logo、名字的字号应该适当缩小一些。

### 搜索框

标题栏里的搜索框的默认外观：

![搜索框-未激活](./搜索框-未激活.png)

激活时的外观：

![搜索框-激活](./搜索框-激活.png)

搜索框左侧有一个灰色的搜索图标，颜色是 `#999`。它不可点击，因为不需要点击它。

当用户在搜索框里输入文字之后，在右侧显示一个灰色的关闭按钮，点击它可以清除输入的搜索词。这个按钮被 hover、点击时颜色会变成高亮颜色。

#### 搜索框的样式

在 PC 端，搜索框的宽度是 `210px`，总高度 `32px`，有 1px 的边框，边框颜色是 `#999`。

当搜索框获得焦点后，边框颜色变成主题色。

搜索框里有 placeholder，文字颜色是 `#999`。用户输入的文字的颜色是 `#666`。

在移动端，宽度是最大宽度的 80%。

### 图标

标题栏里使用到的图标：
- 搜索框左侧的搜索图标：search-in-searchbar.svg
- 搜索框右侧的关闭按钮：close.svg
- 指示展开状态的按钮（箭头向上）：arrow-up.svg
- 指示折叠状态的按钮（箭头向下）：arrow-down.svg
- 赞助按钮（心形）：heart-line.svg
- 关闭按钮：close.svg

## 主体区域

主体区域分为两个大板块，分别是导航栏和内容区域。

### 导航栏

第一个板块用来显示一级分类导航和下载摘要区域。

#### 一级分类导航

一级分类的名字依次是：
- 首页
- 抓取
- 命名
- 下载
- 增强
- 通用
- 帮助
- 搜索

**每个分类承载的内容：**

| 一级导航 | 主要承载内容                                     | 说明                                             |
| -------- | ------------------------------------------------ | ------------------------------------------------ |
| 首页     | 置顶的设置、开始抓取区域、附加功能区域、下载区域 | 该分类里的内容相当于仪表盘首页。该分类会默认显示 |
| 抓取     | 抓取范围、过滤条件、抓取策略、开始抓取           | 对应“决定抓什么”                                 |
| 命名     | 文件名规则、文件夹规则、序号、标签别名           | 对应“决定文件叫什么”                             |
| 下载     | 保存格式、下载线程、下载策略、下载触发           | 对应“决定如何保存和下载”                         |
| 增强     | 缩略图、预览、复制、高亮、界面增强               | 对应“增强使用过程”                               |
| 通用     | 外观、语言、日志、导入导出、管理设置             | 系统级、全局级选项                               |
| 帮助     | 显示提示信息和原本位于下载器底部的帮助按钮       |
| 搜索     | 显示搜索结果                                     |

**补充说明：**
- 每个一级分类名称前面都会显示一个图标。每个名称都有两套图标文件：未激活时显示的是轮廓较细的图标（line 风格，视觉效果比较轻），激活后显示较粗的图标（fill 风格，视觉效果比较重）。
- 点击一级分类就会激活它，使它高亮显示，并在右侧显示该分类里的内容。
- 默认激活的分类是“首页”，它的作用是仪表盘。
- 同时只能激活一个一级分类。
- 需要记录上一次激活的分类、当前激活的分类，在有需要时使用。在内存里保存这些数据即可，不需要持久化保存。当搜索页面隐藏之后（因为用户清除了搜索内容），需要自动切换到上一次激活的分类。至于当前激活的分类，可能会在一些代码逻辑里使用。
- 每当激活左侧的一级分类时，隐藏其他分类的右侧区域，并显示当前分类的右侧区域。还需要检查它里面应该显示哪些设置项。如果某个设置项应该显示在这里但却没有显示（因为它可能被移动到了搜索结果里），就把这个设置项移动过来。
- “搜索”分类默认是隐藏的，所以用户不能主动激活它。只有当需要显示搜索结果时才会激活搜索分类，并在右侧区域显示搜索结果。

##### 样式

每个一级分类在未激活时没有背景颜色，图标、名字的颜色都是 `#111`。
鼠标经过一级分类时，在它下方显示浅灰色的背景颜色作为指示，颜色为 `#eee`。
激活后，图标、名字的颜色都是白色 `#fff`，名字加粗显示。图标从 line 风格变成 fill 风格（即使用两套图标）。此时背景颜色变为主题色，并且显示波纹扩散效果（旧的设置面板里的按钮已经添加了这个动画效果，可以在 `src\style\style.less` 里搜索类名 `.hasRippleAnimation` 查看。不过之前的这个动画效果是 hover 时触发，在一级分类里需要改成点击时触发）。

###### PC 端

导航栏宽度 `190px`，`padding: 0 10px;`，`margin: 15px 0;`。所以中间可用的宽度是 `170px`。

每个一级分类的高度是 `50px`，padding 是 `0 15px`，margin 是 `0 0 10px`。内容垂直居中显示。

图标的宽高是 `26px`，名字在右侧剩余空间里水平居中显示。当下载器的界面语言是简体中文、繁体中文时，由于名字的字数少，显示出来的长度短，为了平衡视觉效果，需要使名字的字间距加大（两个字之间大约有 6px 的间隙）。

###### 移动端

在移动端里，一级分类导航里的元素是横向显示的，可以换行。每个导航元素的宽度、高度都适当减小，最小宽度是 `25%`，允许增长（这是考虑到一些语言里的名字可能比较长，25% 的宽度如果不够用，就允许宽度增长）。

##### 图标

每个一级导航都有两个图标，具体如下：
- 首页：home-line.svg，home-fill.svg
- 抓取：filter-line.svg，filter-filling.svg
- 命名：rename-line.svg，rename-fill.svg
- 下载：download-line.svg，download-fill.svg
- 增强：magic-line.svg，magic-fill.svg
- 通用：setting-line.svg，setting-fill.svg
- 帮助：book-line.svg，book-fill.svg
- 搜索：search-line.svg，search-fill.svg

#### 下载摘要区域

在第一个板块里的导航区域后面有一个独立的区域作为下载摘要栏。

下载摘要区域是默认隐藏的，只有当有抓取结果之后才会显示（即触发了这两个事件之一 `[EVT.list.crawlComplete, EVT.list.resume]`）。

下载摘要栏里有两个板块：
- 第一排显示下载状态图标和下载总进度。下载状态有 5 种状态，分别使用不同的图标。这些状态分别是：未开始下载、下载中、下载已暂停、下载已停止、下载完毕。这一排的图标和文字都是灰色的`#666`。
- 第二排显示 3 个控制按钮，分别使用不同的图标。这些按钮分别是：开始下载，暂停下载，停止下载。用户点击按钮可以控制下载状态（等同于点击下载区域里的对应按钮）。

为了让用户可以确认每个按钮的功能，每个按钮都应该有 title 属性，这样当用户把鼠标放到对应按钮上时可以查看提示。

##### 样式

###### PC 端

在 PC 端页面里，下载摘要区域显示在左下角，也就是会定位到第一个板块的底部。

它具有 `padding:20px 0 20px 10px;`，顶部有一条灰色横线（上边框，颜色是 `#ccc`）。

它里面的两个板块分成上下显示（显示成两排），左侧对齐。

按钮都是从左到右排列的。每个按钮与相邻元素都有 `20px` 的间距。

###### 移动端

在移动端页面里，下载摘要区域显示在一级导航区域的下方，单独占据一行。

使用一个内容卡片来显示它。此时它没有 padding，因为内容卡片自带了 padding。

两个板块的内容显示到一行里。每个按钮之间具有 `16px` 的间距。没有横线（边框）。

##### 图标

下载摘要区域使用的图标如下：
- 开始下载：play.svg
- 下载中：right-arrow.svg
- 暂停下载：pause.svg
- 停止下载：stop.svg
- 下载完毕：complete.svg

### 内容区域

第二个板块是内容区域，用于显示被激活的一级导航的内容。

#### 使用卡片展示内容

内容区域里大量使用各种卡片来显示内容。

每个卡片具有白色背景（当颜色主题为暗色时则是深灰色背景），并添加 padding、margin，使每个按钮区域或设置项都显示为独立的小卡片。

在下方的“卡片的设计”章节里可以查看卡片系统的具体说明。

#### 一些内容可折叠，并保存展开/折叠状态

折叠/展开内容的场景：
1. 内容区域里经常会显示二级分类标题+设置列表的组合。点击二级分类的标题卡片可以折叠/展开它下方的设置列表。
2. 内容区域会显示“标题+内容卡片”（主要是首页的多个按钮区域会使用这种卡片来展示），点击标题可以折叠/展开这个卡片的内容。

有个例外：搜索结果页面里的分类标题是动态组合出来的，它们可以展开/折叠，但不需要保存其状态。

对于需要保存展开/折叠状态的分类和区域，需要把它的展开/折叠状态持久保存到设置里。我在 `src\ts\setting\Settings.ts` 里添加了 `expandedCards` 设置来保存每个区域的展开/折叠状态。在编写卡片的代码时可以修改此设置以持久保存状态。

另外，当用户点击标题栏上的“展开/折叠”按钮时可以切换全局的展开/折叠状态。如果该按钮变成展开状态，那么所有可折叠/展开的内容都会展开，反之则全部折叠。

#### 二级分类的标题卡片需要支持黏性滚动

当内容区域滚动时，如果某个二级分类区域被滚动遮挡了一部分内容（即上半部分在显示区域之外），那么该二级分类的标题卡片会始终显示在内容区域的顶部，不会被滚动到显示区域之外。

只对展开状态的二级分类标题使用黏性滚动；如果二级分类被折叠，就不为它使用黏性滚动。

注意：保持只有一个标题卡片是处于黏性滚动的。如果有多个二级分类区域被滚动到显示区域之外，只显示它们里的最后一个区域的标题卡片，因为这个标题就是当前正在查看的内容的标题。

准确来说，**只有“标题卡片”才会在展开状态时使用黏性滚动**。其他两种类型的卡片不使用黏性滚动。下面是对一些情况的补充：
- 首页的抓取按钮区域、附加功能区域、下载区域不是“标题卡片”，所以不使用黏性滚动。
- 搜索结果页面里的分类标题是“标题卡片”，所以需要应用黏性滚动。

#### 首页

“首页”里聚合了置顶的设置、重要的按钮、下载区域，起到仪表盘的作用。

具体内容有：

##### 帮助区域

用于显示重要的提示、用户在使用前非常有必要看到的提示。

每条提示会使用“内容卡片”来展示，此时的内容卡片是一级卡片（不属于某个二级分类）。

每条提示都有对应的“确认”按钮，如果用户点击了这个按钮，就会隐藏这条提示。可以参考 `src\ts\setting\FormHelpManager.ts` 里的 `displayTipArea` 方法，因为新版设置面板里显示的依然是这几条帮助，只是表现形式和所属的内容区域变化了。

##### 置顶的设置

集中显示被置顶的设置。

内容是一个二级标题卡片（名字为“置顶的设置”）+ 被置顶的所有设置项。

该二级分类默认折叠。

##### 开始抓取

使用“标题+内容卡片”显示原本“抓取”选项卡里的按钮（可以复用之前的插槽元素）。

该区域默认展开。

开始抓取按钮区域在原本的 `src\ts\setting\FormHTML.ts` 里是这些代码：

```html
<div class="centerWrap_btns">
 <slot data-name="stopCrawl"></slot>
 <slot data-name="crawlBtns"></slot>
 <slot data-name="selectWorkBtns"></slot>
</div>
```

##### 附加功能

使用“标题+内容卡片”显示原本“更多”选项卡里的按钮（可以复用之前的插槽元素）。

该区域默认折叠。

附加功能区域在原本的 `src\ts\setting\FormHTML.ts` 里是这些代码：

```html
<div class="centerWrap_btns">
 <slot data-name="otherBtns"></slot>
</div>
```

##### 下载区域

使用“标题+内容卡片”显示原本“下载”选项卡里的按钮和下载进度区域（可以复用之前的插槽元素）。

该区域默认折叠。当有抓取结果之后会自动展开。

下载区域在原本的 `src\ts\setting\FormHTML.ts` 里是这些代码：

```html
<div class="centerWrap_btns">
 <slot data-name="exportResult"></slot>
 <slot data-name="namingBtns"></slot>
</div>
<slot data-name="downloadArea"></slot>
<slot data-name="progressBar"></slot>
```

##### 图标

首页里每个卡片标题使用的图标：
- 置顶的设置：pin-line.svg
- 开始抓取：rocket.svg
- 附加功能：features.svg
- 下载区域：download-line-2.svg

#### 抓取

抓取分类的内容由多个二级分类标题卡片 + 设置卡片组成。

| 二级分类   | 设置项                                                                                                                                                               |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 抓取范围   | 抓取多少作品、抓取多少页面、抓取每个用户最新的几个作品                                                                                                               |
| 作品类型   | 作品类型、年龄限制、AI作品、原创作品、图片色彩、图片数量                                                                                                             |
| 作品数据   | 收藏状态、收藏数量、图片的宽高、图片的宽高比例、id范围、投稿时间                                                                                                     |
| 标签与标题 | 必须含有tag、不能含有tag、标题必须含有、标题不能含有                                                                                                                 |
| 多图作品   | 多图作品的图片数量上限、多图作品只抓取前几张图片、多图作品只抓取后几张图片、多图作品不抓取前几张图片、多图作品不抓取后几张图片、特定用户的多图作品不下载最后几张图片 |
| 屏蔽用户   | 用户阻止名单、针对特定用户屏蔽标签                                                                                                                                   |
| 执行策略   | 不抓取下载过的作品、减慢抓取速度、定时抓取的间隔时间、自动导出抓取结果、导出ID列表                                                                                   |

#### 命名

命名分类的内容由多个二级分类标题卡片 + 设置卡片组成。

| 二级分类           | 设置项                                                                                                                                                                                         |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 文件夹和文件的名字 | 图像作品的命名规则、小说的命名规则、在不同的页面类型中使用不同的命名规则、如果作品含有某些标签则对这个作品使用另一种命名规则、合并系列小说时的命名规则、标签分隔符号、日期格式、文件名长度限制 |
| 调整文件夹         | 不创建文件夹、为多图作品添加一层文件夹、为r18作品添加一层文件夹、使用第一个匹配的标签建立文件夹                                                                                                |
| 序号               | 序号起始值、第一张图不带序号、在序号前面填充0                                                                                                                                                  |
| 别名               | 标签别名、自定义用户名                                                                                                                                                                         |
| 移除特殊字符       | 移除文件名里的emoji、移除用户名中的at和后续字符                                                                                                                                                |

#### 下载

下载分类的内容由多个二级分类标题卡片 + 设置卡片组成。

| 二级分类 | 设置项                                                                                                                                                                             |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 下载行为 | 下载线程、自动开始下载、下载之后收藏作品、下载间隔、点击收藏按钮时下载作品、点击点赞按钮时下载作品、文件下载顺序、文件体积限制、把文件保存到用户上次选择的位置、下载完成后显示通知 |
| 下载记录 | 管理下载记录、不下载重复文件、在已下载的作品上显示边框                                                                                                                             |
| 图片尺寸 | 图片尺寸                                                                                                                                                                           |
| 动图     | 动图保存格式、同时转换多少个动图                                                                                                                                                   |
| 小说     | 小说保存格式、在小说里保存元数据、下载小说的封面图片、下载小说里的内嵌图片、自动合并系列小说、合并系列小说时的分割阈值                                                             |
| 元数据   | 保存作品的元数据、保存作品的简介                                                                                                                                                   |

#### 增强

增强分类的内容由多个二级分类标题卡片 + 设置卡片组成。

| 二级分类       | 设置项                                                                                           |
| -------------- | ------------------------------------------------------------------------------------------------ |
| 预览           | 预览作品、长按右键显示大图、预览作品的详细信息                                                   |
| 缩略图         | 显示更大的缩略图、替换方形缩略图以显示图片比例、在多图作品页面里显示缩略图列表、把图片显示为灰色 |
| 缩略图上的按钮 | 缩略图上按钮的位置、在作品缩略图上显示放大按钮、在作品缩略图上显示下载按钮、复制按钮             |
| 其他           | 高亮关注的用户、收藏设置                                                                         |
| 搜索页面       | 在搜索页面添加快捷搜索区域、过滤搜索页面的作品、在搜索页面里移除已关注用户的作品、预览搜索结果   |

#### 通用

通用分类的内容由多个二级分类标题卡片 + 设置卡片组成。

| 二级分类 | 设置项                                             |
| -------- | -------------------------------------------------- |
| 外观     | 颜色主题、背景图片、选项卡切换方式、高亮显示关键字 |
| 语言     | Language                                           |
| 日志     | 日志区域的默认可见性、导出日志                     |
| 管理设置 | 管理设置                                           |

#### 帮助

帮助分类里保存的不是设置，而是提示信息和多个帮助按钮。

第一部分是一些独立的提示消息卡片，使用“内容卡片”展示，每个卡片都占据一行。

接下来是第二个板块，用于显示多个小按钮，每个元素都使用内容卡片，并且这些卡片都是只有部分宽度的。

这些按钮大部分来自旧版界面底部的对应元素。具体有：
- 使用手册
- 常见问题
- 获取帮助
- 最近更新
- GitHub
- Fanbox 下载器
- 机场推荐（该按钮只在下载器的界面语言为简体中文时显示）
- 赞助我
- 第三方库（点击之后使用 MsgBox 显示使用的开源 JS 库列表，并在末尾注明当前界面布局受 [Clash Verge](https://github.com/clash-verge-rev/clash-verge-rev) 启发）

之前有一些按钮是超链接（使用手册和 GitHub），点击就会直接跳转。在新版设置面板里，把所有超链接都改成按钮，不会直接跳转了。这样统一了按钮的行为，不会导致用户遇到预期之外的行为。

点击按钮之后会使用 MsgBox 显示详细的内容。

##### 样式

在 PC 端，按钮一行显示 4 个。在移动端可以适当减少些，一行显示 2 个。但如果内容太长（主要是英语里一些单词可能太长），允许按钮增加宽度。

按钮里的图标和文字挨在一起，水平、垂直居中显示。图标的宽高是 `24px`，颜色是 `#666`。图标与文字之间有 `6px` 的间隙。

按钮的背景颜色是白色，是一级卡片，具有一级卡片的样式。

鼠标 hover 时，背景颜色变成蓝色 `#09a0f4`，图标和文字则变成白色 `#fff`。点击按钮时会触发波纹扩散的动画效果。

#### 图标

帮助里的按钮用到了这些图标：
- 使用手册：wiki.svg
- 常见问题：help.svg
- 获取帮助：get-help.svg
- 最近更新：new-2.svg
- GitHub：github.svg
- Fanbox 下载器：F.svg
- 机场推荐：paper-airplane.svg
- 赞助我：heart-line.svg
- 第三方库：list.svg

#### 搜索

用于显示搜索结果。它的内容可以参考下方的“如何呈现搜索结果”章节。

### 样式

主体区域有最大高度限制，如果内容超过高度限制则会显示滚动条。

主体区域有 1px 的边框，颜色是 `#9dd4f9`。有投影 `0 0 25px #b4e0f5`。

#### PC 端

在 PC 端页面里，两个大板块是左右并排显示的。即左侧是一级导航和下载摘要栏，右侧是内容区域。

当高度不足时，左右两个板块都可以上下滚动。因为这两个板块是分开的，所以它们的滚动也是独立的，一侧板块滚动时，另一侧不能跟着滚动。

高度：
内容区域的最大高度 75 vh。在逻辑分辨率 1920x1080 的浏览器里，内容区域的高度上限是 810 px，所以总高度最大是 870px。

宽度：
- 左侧导航区域的总宽度为 `190px`，左右两侧有 `10px` 的 padding。
- 右侧区域的总宽度为 `735px`，左侧有 `15px` 的 padding，右侧则是 `20px` 的 padding（因为右侧可能会有滚动条，所以 padding 比左侧大一些）。
- 主体区域的总宽度是 `925px`。

#### 移动端

在移动端页面里，主体区域里的两个大板块是上下布局：

- 第一个板块显示在上方。一级导航单独占据一行，内容横向排列，一字排开，如果一行放不下就会自动换行。下载摘要栏也是单独占据一行，内容横向排列。
- 第二个板块显示在下方。

在移动端，第一个板块没有高度限制，不会滚动。只有第二个板块会因为高度不够而滚动。

## 卡片的设计

### 卡片的视觉效果要轻

卡片要轻。因为设置数量很多，所以每个卡片的视觉效果不应该太重。现在卡片的样式是：
  - 背景是白色 `#fff`
  - 没有边框
  - 卡片具有较轻的投影。一级卡片的投影是 `0 0 15px #ddd`，二级卡片的投影范围小一些，是  `0 0 10px #ddd`。
  - 主要靠留白和分组来区分，每个卡片之间有一定间距。

### 卡片的种类

下面的每种卡片都带有示意图。这些示意图不是精确的设计稿，仅供参考。

每种卡片的宽度都是自适应的。默认会占满可用宽度，但也可以通过设置样式使其只占据部分宽度。

#### 标题卡片

用于显示二级分类的名字。

左侧是图标和分类名字。图标有可能有，也可能没有。没有图标的话就不显示图标的元素。
右侧有一个箭头图标表示展开、折叠状态。其实它只是同一个图标，箭头是向下的。当这个分类折叠时，正常显示这个图标（旋转角度为 0）；当这个分类展开时，把这个图标旋转 180 度，使箭头向上。

事件：
点击标题卡片会切换其下内容区域的展开/折叠状态。不需要单独为箭头图标绑定事件，因为点击整个标题卡片都可以展开/折叠。

##### 样式

它总是一级卡片，具有 `margin-top: 15px;` 和 `padding: 8px 15px;`，总高度为 `50px`。背景颜色是白色，圆角 `8px`。

分类名字和箭头是左右两端布局。其中的元素在水平轴上居中对齐。

图标容器的宽高是 `32px`，背景颜色 `#eef7fd`，圆角 `6px`。内部的图标宽高为 `20px`，图标颜色是主题色，居中显示。

右侧箭头图标的文件是 `arrow-down-2.svg`，宽高为 `16px`。

#### 内容卡片

用于直接显示内容的卡片。没有标题、箭头等结构，是很简单的卡片。它被用来显示多种内容，例如提示信息、设置项、帮助分类里的按钮。

内容卡片可能是一级卡片，也可能是二级卡片：
- 如果它不隶属于二级分类，那么就是一级卡片，例如首页顶部的帮助信息、帮助页面里的小卡片。
- 当它用于显示设置项时，如果它隶属于二级分类，那么它是二级卡片，它的宽度会比标题卡片稍微窄一些，以表示层级关系。这应该可以通过在父元素上设置样式来实现。
- 当它用于显示设置项时，该设置可能具有置顶属性、new 属性。这两种属性有不同的表现形式，具体可以查看该文档底部的“置顶设置功能”和“新增设置的表现形式”章节。

##### 样式

如果它是一级卡片（没有显示在标题卡片下）具有 `margin-top: 15px;`，如果是二级卡片则具有 `margin-top: 10px;`。它的 padding 总是 `padding: 12px 15px;`。背景颜色是白色，圆角 `12px`。

如果它是二级卡片，那么宽度比一级卡片少 `20px`，就是左右两侧各少 `10px`，并且保持居中。

字号是 `14px`，行高 `26px`。

由于内容卡片里主要显示设置项，并且设置项的 html 代码没有大的改变，所以可以优先复用之前的样式。

#### 标题+内容卡片

相当于一个独立的板块，上方是标题栏，下方的内容区域里可以放置任意内容。

标题栏：内容与“标题卡片”相同，左侧是图标和名字，右侧有一个箭头图标表示展开、折叠状态。

目前我打算用它来显示首页仪表盘里的开始抓取、附加功能、下载管理区域。

##### 样式

它总是一级卡片，具有 `margin-top: 15px;` 和 `padding: 0;`。背景颜色是白色，圆角 `8px`。

标题栏的样式可以复用“标题卡片”的样式，区别在于：
- 此处的标题栏没有 margin，因为其父元素已经设置了 margin。
- 此处的标题栏没有圆角，因为其父元素已经设置了圆角。
- 此处的标题栏有底边框，宽 `1px`，颜色 `#ececec`。

内容区域具有 `padding: 15px 15px;`。

## 搜索功能

### 搜索匹配范围

- 从所有设置项的配置数据里查找（而不是从 DOM 元素里查找）。
- 搜索时不区分大小写。
- 不搜索被隐藏的设置，因为它们不会显示，所以不应该搜索它们。
- 优先从每个设置项的名字（`name` 属性）里匹配搜索词。如果名字有匹配结果，就不再查找它的搜索词列表 `searchWords`。当这个设置项的名字没有匹配的结果时才会在它的搜索词列表里查找。
- 从搜索词匹配时，依次使用两种匹配模式：第一种是 `searchWord.includes(inputWord)`，这适合用于“搜索词的文本覆盖了用户输入的文本”的情况。但有些关键词比较短，如果用户输入的搜索词比较长，`searchWord.includes` 就匹配不到，例如用户输入了 `修改语言`，但搜索词里只有更短的 `语言`。所以如果第一种匹配模式没有结果，就使用第二种匹配模式 `inputWord.includes(searchWord)` 反过来匹配一次。
- 由于搜索词列表 `searchWords` 是数组，所以在从某个设置里进行搜索时，一旦有匹配的搜索词，就不再匹配剩余的搜索词，因为没有必要了。
- 不查找设置的子选项。因为设置项的名称与搜索词的关联度是最高的，子选项则不够高。有些子选项会出现在多个设置里，但这些设置的作用差别很大，所以没有太大必要对子选项进行搜索。这样也可以减少工作量，例如：1. 如果要搜索子选项的话，就需要在配置数据里添加子选项的文本，而且数量很多。2. 有些设置是未启用的，如果搜索子选项的话，是否要搜索未启用的设置的子选项？这是个选择。如果进行搜索，还会面临进一步的问题：对于未启用的设置，其子选项区域往往是隐藏的，如果在隐藏的子选项里找到了搜索结果，要让用户看到这些子选项，就需要临时把子选项显示出来。这也是个麻烦的地方，所以不处理。
- 不查找“帮助”里的内容，因为它里面的内容不属于设置。

### 如何呈现搜索结果

- 用户输入搜索词时，自动查找并显示搜索结果。为了避免短期内多次计算的压力，需要使用防抖处理（等用户停止输入 0.2 秒之后才开始查找搜索结果）。
- 不在搜索框下面显示下拉的候选项列表，因为这样会增加复杂度。而且目标设置本身就可以在设置面板里显示，所以没太大必要显示下拉候选项。
- 当用户在搜索框里输入内容之后（搜索词非空），就激活“搜索”分类，并在其右侧内容区域（全局搜索页面）里显示符合条件的设置。
- 搜索页面的顶部会显示有多少条搜索结果。如果没有符合条件的搜索结果，显示“没有找到符合条件的设置。你可以尝试搜索更简短的关键词。”。如果有搜索结果，则显示`找到 x 条与“搜索词”有关的设置`。这个区域的样式：`margin:15px 0 0 5px`，文字从左到右显示，颜色是 `#444`。
- 把搜索到的设置项从它们的二级分类区域里移动到搜索结果区域里。这些设置项可以直接使用，所以点击它们时，不需要跳转到它们原本所属的一级分类里。
- 在搜索到的设置里，如果设置的标题里有命中的文字，则高亮显示命中的文字，使其具有黄色背景颜色。目前使用黄色 `#ffe74b`。
- 搜索结果可能会来自多个一级分类，在呈现搜索结果时，这些设置的显示顺序应该与左侧一级导航一致：依次是：首页 → 抓取 → 命名 → 下载 → 增强 → 通用。
- 在每个一级分类内部，再按原本的二级分组顺序显示。也就是以“一级分类”+“二级分类”为搜索结果进行分组，例如 `下载 / 小说`，并使用标题卡片来显示这个组合的分类名称。

何时隐藏搜索页面：
未搜索或搜索词为空时，隐藏搜索分类页面，并重新激活上一次激活的一级分类。搜索词为空的情况可能由多种触发方式：
- 当用户点击搜索框右侧的 x 图标时会清除搜索词
- 当用户手动清空了搜索词时（例如连续按 backspace 键）。

可以通过监控搜索框 input 控件的 value 来检测搜索词是否为空。

## 置顶设置功能

被置顶的设置会集中显示在“首页”分类的内容区域里。

#### 置顶图标的外观

被置顶的元素以前会在其左侧显示一个置顶图标（📌），现在改为在其卡片**内部**显示边框，粗 `2px`，颜色是 `#bde2ff`。这个边框具有与内容卡片一致的圆角。这样被置顶的设置设置会变得更加醒目，界面整体上也更加美观。

#### 怎么置顶和取消置顶

在设置项的卡片上长按一段时间（例如 0.5 秒），就可以切换该设置的置顶状态。旧版的代码里有对应的逻辑，可以参考。

#### 被置顶的设置只有一份元素

关于被置顶的设置如何显示，下面是具体说明：
- 当左侧导航区域激活了“首页”页面时：查找所有被置顶的设置，把它们的元素移动到“首页”的置顶设置区域里。
- 当左侧导航区域激活了非“首页”页面时（包括搜索结果页面）：如果需要显示置顶的设置，就查找其元素。如果元素不在当前页面里（常见的原因是显示到了“首页”的置顶区域里，或者搜索结果里），就把这些元素移动到当前页面里，并按照 `src\ts\setting\OptionConfigs.ts` 里的 `options` 配置使其显示到正确的位置（即让设置按固定的前后顺序显示）。这样，用户在其他分类里依然能看到被置顶的设置，不会产生“这个设置怎么不见了”的困惑。

这样做的目的是为了让置顶的设置只有一份元素，而非多份。原因是下载器在设计时没有考虑到一个设置会存在多份的情况。如果一个设置存在多份，会带来问题：
- 由于设置项都包裹在一个 form 表单里，并且使用 form.key 的方式来读取、设置对应的值。如果被置顶的设置存在多份，那么同一个 key 会出现多次，造成问题。
- 一些模块需要查找特定的设置元素并对其进行操作（如读取、写入一些控件的值，或者操作里面的 DOM）。如果有多个相同的设置，就需要遍历这些重复的设置来挨个处理，并且同步数据和元素，会比较麻烦。

## 新增设置的表现形式

新增设置的处理逻辑在 `src\ts\setting\ShowNewIcon.ts` 里，现在只需要修改表现形式。

之前新设置会在它的左侧显示一个 ✨ 符号，现在改为在卡片的右上角显示一个 new 徽章，对应的图标是 `new.svg`。

## 独立的提示消息卡片

设置面板里有一些重要的提示，使用单独的元素显示，占据一行。

在旧版界面里，就是 `src\ts\CenterPanel.ts` 里的 `p id="tipPinOption"` 元素。它的内容由 3 部分组成：
1. 前面的灯泡图标 💡
2. 提示内容的文本
3. “我知道了”文字按钮，点击之后不再显示这条提示。

现在这些重要提示改为使用独立的内容卡片来显示。在“帮助”页面的设计稿里，顶部的 3 个卡片就是重要提示：

![](./设计图-帮助.png)

每个提示的内容依然是 3 部分，但发生了一些变化：
1. 灯泡图标💡从 emoji 符号变成了 svg 图标（图标文件是 `light-line.svg`）。图标是灰色的 `#666`。
2. 提示内容没有变化，依然是纯文本
3. “我知道了”按钮从文字变成了一个对号 svg 图标（图标文件是 `yes_submit.svg`），title 为“已确认”。

这个对号图标的样式：
- 宽 `30px`，高 `20px`，颜色是主题色，圆角 `4px`。
- 当鼠标 hover 该图标时，显示背景颜色 `#e5f2ff`。

灯泡图标和对号图标都需要与文字行垂直居中对齐。
